---
name: gantt-slide
description: >
  根據里程碑清單或專案時間表，在簡報中產出 Gantt 圖 slide（HTML canvas 或 SVG，可嵌入 PPTX）
  TRIGGER when: when working with gantt-slide
when_to_use: when working with gantt-slide
version: 1.0.0
tags: [docs]
languages: all
source: harvest-auto
---

# gantt-slide

## Overview

根據里程碑清單或專案時間表，在簡報中產出 Gantt 圖 slide（HTML canvas 或 SVG，可嵌入 PPTX）

## Background

From session harvest analysis:

Session 3（sabre）明確需要甘特圖 slide，且甘特圖的 HTML 實作（日期範圍計算、列佈局、顏色標記）是每次重複的繁瑣工作。目前 `office-pptx` 無法自動從里程碑列表產生甘特圖；`writing-plans` 只輸出 Markdown，不生成視覺化 slide。

## TODO

This skill was auto-generated from a harvest candidate.
Fill in the implementation details, patterns, and examples.
