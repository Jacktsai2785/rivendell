---
name: 4-github-pages-corporate-site
description: >
  用 CNAME + DESIGN.md + STATE.md 模式建立企業官網（靜態 + 自訂域名 + GitHub Pages）。
  TRIGGER when: 「做官網」「靜態企業網站」「GitHub Pages 自訂域名」。
when_to_use: 「做官網」「靜態企業網站」「GitHub Pages 自訂域名」。
version: 1.0.0
tags: [N/A]
languages: all
source: harvest-auto
---

# 4. github-pages-corporate-site

## Overview

用 CNAME + DESIGN.md + STATE.md 模式建立企業官網（靜態 + 自訂域名 + GitHub Pages）。

## When to Use

「做官網」「靜態企業網站」「GitHub Pages 自訂域名」。

## Background

From session harvest analysis:

Session 1 雖大（200 訊息），但已被 `gstack-design-consultation` + `gstack-design-html` + `frontend-design` 覆蓋；CNAME / GH Pages 部署是 1 行設定，不需獨立 skill。

## TODO

This skill was auto-generated from a harvest candidate.
Fill in the implementation details, patterns, and examples.
