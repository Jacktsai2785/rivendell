---
name: audio-transcription
description: >
  在 Web App 中加入音訊上傳 → 語音辨識 → 逐字稿顯示的完整流程
  TRIGGER when: when working with audio-transcription
when_to_use: when working with audio-transcription
version: 1.0.0
tags: [backend / workflow]
languages: all
source: harvest-auto
---

# audio-transcription

## Overview

在 Web App 中加入音訊上傳 → 語音辨識 → 逐字稿顯示的完整流程

## Background

From session harvest analysis:

| Session 14：建立了 `audio-transcription-flow.md`、`audio-transcription.html`、`audio-transcription.md`，顯示完整的 UI + API + 轉換流程設計，且是跨前後端的固定模式 |
| **與現有 skills 差異** | 現有 `imap-smtp-integration` 類似「協議整合」模式，但無音訊處理 skill |

## TODO

This skill was auto-generated from a harvest candidate.
Fill in the implementation details, patterns, and examples.
