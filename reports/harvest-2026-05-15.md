# Session Harvest — 2026-05-15

## 摘要

| 項目 | 數值 |
|------|------|
| Sessions | 10 |
| Messages | ~62 |
| 總成本 | $0.00 |
| 涉及專案 | rivendell, taiwan-company |
| 主要工具 | Bash(14), Read(14), WebSearch(12), ToolSearch(9), Skill(1), Write(1) |
| 使用 Skills | workflow-retro（session 1） |

---

## Session 逐條摘要

| # | 專案 | Msgs | 主要活動 | 工具組合 |
|---|------|------|---------|---------|
| 1 | rivendell | 32 | 執行 workflow-retro skill，讀取 5/9–5/13 harvest 與 LEARNINGS，產出週回顧報告 | Bash(14), Read(14), Skill(1), Write(1) |
| 2–10 | taiwan-company | 3–4 each | 以 headless agent 方式搜尋「光寶科技股份有限公司」（統編 23357403）官方網站，每次各自用 ToolSearch 載入 WebSearch schema 後執行搜尋，僅輸出 URL | ToolSearch(1), WebSearch(1–2) each |

**值得注意：** Sessions 2–10 是針對**同一家公司**執行 9 次幾乎完全相同的搜尋。
這強烈暗示上游批次排程在發送 job 前未做去重（dedup），導致相同輸入被重複處理。

---

## Skill 候選清單

### 1. `tw-company-website-finder` 實作補全

**評級：Strong**

| 欄位 | 內容 |
|------|------|
| 名稱 | tw-company-website-finder |
| 目的 | 給定台灣公司名稱與統一編號，以 WebSearch 找出最可能的官方網站 URL，僅輸出 URL 字串 |
| 觸發時機 | headless agent 收到 `(公司名稱, 統編)` pair 需要找官網；或其他 tw-company 系列 skill 需要官網欄位時 |
| 分類 | workflow |
| 現況 | `skills/workflow/tw-company-website-finder/SKILL.md` **已存在但為 stub**（auto-generated, TODO 未填） |

**理由：**
- Sessions 2–10 共 9 次都是這個原子步驟的實際執行記錄，格式高度一致：
  ```
  請用 WebSearch 搜尋「{公司名稱}」（統編：{統編}）的官方網站。
  只輸出最可能的官方網站 URL（含 https://），禁止任何其他說明文字。
  若找不到，輸出空字串。
  ```
- ToolSearch 每次都需要先載入 WebSearch schema，說明這個步驟固定需要 schema preload。
- 實作非常明確，可直接將上述 prompt 模板寫入 SKILL.md 的 Instructions 段落。

**行動項目：** 填入 `tw-company-website-finder/SKILL.md` 的實作說明，包含：prompt 模板、schema preload 步驟、輸出格式（純 URL 或空字串）。

---

### 2. `batch-job-dedup-guard`（前置去重檢查）

**評級：Weak**

| 欄位 | 內容 |
|------|------|
| 名稱 | batch-job-dedup-guard |
| 目的 | 批次 job 發送前對輸入清單做去重，避免相同 payload 被重複處理 |
| 觸發時機 | 任何以 headless agent 批次處理公司/實體清單前 |
| 分類 | meta |

**理由：**
- Sessions 2–10 完全相同的輸入被執行 9 次，浪費資源且會產生重複結果。
- 但這是**基礎設施/排程邏輯問題**，更適合直接修 upstream 批次腳本，而非建立獨立 skill。
- 目前已有 `headless-agent`、`dispatching-parallel-agents` 等相關 skills，此議題更適合作為這些 skills 的 checklist 項目。

**行動項目（建議）：** 在 `headless-agent` 或 `dispatching-parallel-agents` 的 SKILL.md 中加入「批次前先去重輸入清單」的 checklist 步驟，不需要獨立建 skill。

---

## 重複模式觀察

### ToolSearch-before-WebSearch（已知慣例）
所有 taiwan-company sessions 都先用 `ToolSearch` 載入 WebSearch schema，再執行搜尋。
這是 headless agent 的標準 schema preload 步驟，屬於已知慣例，無需另立 skill。

### workflow-retro 讀取模式
Session 1 展現的完整讀取路徑：`LEARNINGS.md` → 多個 `harvest-*.md` → Bash 統計 → Write 報告。
這個模式與現有 `workflow-retro` skill 的設計完全吻合，skill 運作正常。

---

## 本次 Harvest 行動清單

- [ ] 填寫 `skills/workflow/tw-company-website-finder/SKILL.md`（實作補全，Strong 候選）
- [ ] 調查 taiwan-company 批次排程，修復同公司被送 9 次的去重問題
- [ ] 在 `headless-agent` SKILL.md 加入「批次前去重輸入」提示（選配）
