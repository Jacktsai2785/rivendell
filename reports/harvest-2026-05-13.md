# Session Harvest — 2026-05-13

## Session 摘要

- **日期**：2026-05-13
- **涵蓋範圍**：13 個 session，約 268 則訊息
- **主要專案**：
  - `-home-jacktsai`（主資料夾，227 則訊息——`jk_nb` 個人知識庫建構）
  - `-home-jacktsai-taiwan-company`（11 個 session——產業新聞分類 + PE 盡職調查）
  - `-home-jacktsai-jk-nb`（10 則訊息——知識庫筆記擴增）
- **工具使用**：Bash(71)、Edit(43)、Read(33)、Write(26)、WebSearch(15)、WebFetch(11)、TodoWrite(10)、Agent(6)
- **關鍵活動**：
  1. 建構 Karpathy 風格 LLM Wiki 個人知識庫（`raw/` + `wiki/` + `inbox.md` + headless agent + systemd timer + Windows 端 capture）
  2. 多次台灣產業新聞分類與週度摘要（前瞻科技 / AI / 消費生活 / 循環經濟 / 綠色永續）
  3. 一次 PE 投資人視角的台灣公司初步盡職調查（臥龍股份，已有 `tw-company-pe-memo` 覆蓋）
  4. 跨專案知識掃描與 Claude Code + Obsidian CLI 整合調查

---

## Skill 候選

### 1. **llm-wiki-knowledge-base** —— Strong ⭐

- **Purpose**：依 Karpathy 的 LLM Wiki / Append-and-Review 模式，從零建立個人知識庫。包含資料夾結構（`raw/` 投擲區、`wiki/` 編譯區、`inbox.md` 即時 append）、四個 agent 動詞（`consume` / `link` / `lint` / `digest`）、VSCode + Foam 配置、以及把 Claude Code 設定為自動載入 `CLAUDE.md` 的 vault-aware 模式。
- **Trigger**：使用者說「建知識庫」「LLM Wiki」「Karpathy 筆記法」「Append and Review」「個人 wiki」「Obsidian + Claude Code」「raw to wiki pipeline」「knowledge vault」。
- **Category**：`workflow/`（或 `backend/`，因為涉及目錄 schema 設計）
- **Rationale**：Session [13] 花了 227 則訊息建構這個系統，產出 6 份 HOWTO 文件、CLAUDE.md、tools/ 結構，是極度可重用的高價值 pattern。目前 `markdown-file-ssot` 只覆蓋 SSOT 查詢用途，沒有任何 skill 處理「個人知識庫 + Karpathy 動詞 + Foam 整合」這條路線。

---

### 2. **headless-agent-knowledge-pipeline** —— Strong ⭐

- **Purpose**：把 Claude Code（`claude --print`）包成 cron-style headless 任務，搭配 systemd user timer 定期消化知識庫——每晚 `digest` inbox.md、每週 `lint` 整個 vault、`consume` 處理新進 raw 檔。包含 unit file 範本、`loginctl enable-linger` 設定、手動觸發 fallback、log rotation。
- **Trigger**：使用者說「定期跑 Claude」「headless Claude」「systemd timer claude」「每晚自動整理筆記」「cron Claude Code」「unattended agent」。
- **Category**：`workflow/`
- **Rationale**：與既有 `headless-agent`、`systemd-user-service`、`launchd-agent` 三個 skill **相關但不重疊**——它們各自處理通用 headless / Linux systemd / macOS launchd，但都沒有處理「headless Claude Code + 知識庫動詞 pipeline」的組合配方。Session [13] 中 `tools/headless-agent` + `tools/systemd/` 是完整的可複製模板。

---

### 3. **raw-watcher-file-ingestion** —— Moderate

- **Purpose**：建立檔案 watcher（inotify / systemd path unit）監控 `raw/` 投擲區，新檔案進來時自動觸發 Claude `consume`。包含 watcher script、systemd path/service pair、debounce、failure retry。
- **Trigger**：使用者說「自動觸發」「監控資料夾」「raw watcher」「file drop trigger」「inotify Claude」。
- **Category**：`backend/`
- **Rationale**：HOWTO_WATCHER.md 是獨立模組，價值不限於知識庫場景——任何「檔案落地→自動處理」流程都用得到。但範圍小、與候選 #2 高度耦合，可以是 #2 的子模組或獨立小 skill。

---

### 4. **windows-clipper-to-wsl** —— Moderate

- **Purpose**：在 Windows 端建立瀏覽器書籤、PowerShell handler、URL scheme（`nbsend://`），把網頁文章/YouTube/截圖透過 bookmarklet → PowerShell → WSL `raw/` 落地。包含 web-clipper template（general / youtube）、PowerShell registration、`register-nbsend.ps1` 安裝流程。
- **Trigger**：使用者說「Windows 抓網頁」「bookmarklet 存筆記」「web clipper WSL」「browser to vault」「URL scheme handler」。
- **Category**：`workflow/`（或新增 `windows/` 分類）
- **Rationale**：Session [13] 明確要求「會開在 Windows 端」+「自動觸發」，產出 `tools/windows/` 一整套 PowerShell + bookmarklet 配置。跨 OS 整合的場景在現有 skills 中沒有覆蓋。但用戶族群可能較窄（WSL + Windows 雙開使用者），故定 Moderate。

---

### 5. **taiwan-news-weekly-digest** —— Moderate

- **Purpose**：把過去 5-7 天的台灣產業新聞清單，彙整成「每日主題 + 高頻標題」週度摘要報告。輸出格式：`2026-05-04 | 主題：X、Y、Z | 高頻標題：...`。
- **Trigger**：使用者說「本週產業新聞摘要」「過去 7 天高頻標題」「每日主題」「週度產業 digest」。
- **Category**：`workflow/`
- **Rationale**：Session [4][7][9][10][11] 共 5 次出現相同的「過去 N 天每日摘要 + 高頻標題」結構。現有 `taiwan-news-classifier` 只負責**單日**分類（相關/不相關 + 子類別），週度彙整是不同的輸出契約。建議作為 `taiwan-news-classifier` 的姐妹 skill 或合併進去成為第二個 mode。

---

### 6. **cross-project-claude-audit** —— Weak

- **Purpose**：掃描本機所有使用過 Claude Code 的專案（`~/.claude/projects/` 索引、git repos、CLAUDE.md 分布），輸出「我電腦上有哪些 Claude 專案、它們在做什麼、最近活躍度」報告。
- **Trigger**：使用者說「我電腦上的 Claude 專案」「跨專案盤點」「Claude project audit」「掃描所有 vault」。
- **Category**：`meta/`
- **Rationale**：Session [13] 開頭明確問「請你幫我調查我的電腦裡面所有使用 claude code 的專案背景」，這是一次性 audit 而非定期任務。價值存在但頻次低，且可能與 `workflow-retro`、`agent-observability` 的範疇邊界模糊。先觀察是否再次出現。

---

### 7. **google-doc-transcript-to-note** —— Weak

- **Purpose**：把 Google Docs 匯出的逐字稿（如 `2026-05-13-完整逐字稿-md-google-文件.md`）清洗成結構化筆記——擷取講者、主題、引用、可行動條目，落地到 `raw/` 或 `wiki/`。
- **Trigger**：使用者說「逐字稿整理」「Google Doc 轉筆記」「meeting transcript 清洗」「演講筆記」。
- **Category**：`docs/`
- **Rationale**：只在 Session [1][13] 出現一次。與 `audio-transcription-flow`（音檔→逐字稿後端）互補，但目前證據單薄。若再出現第二次再升等。

---

## 已被現有 skill 覆蓋（不另立）

| 觀察到的活動 | 已存在 skill |
|---|---|
| 臥龍股份 PE 盡職調查（Session [8]） | `workflow/tw-company-pe-memo` |
| 單日台灣產業新聞分類（Session [2][3][5][6][12]） | `workflow/taiwan-news-classifier` |
| 個別 session 收割 | `meta/session-harvest` |

---

## 建議下一步

1. **優先實作 `llm-wiki-knowledge-base`**（候選 #1）——範圍清楚、模板現成、跨用戶價值高，可直接從 `~/jk_nb/` 提取 6 份 HOWTO 作為 references。
2. **候選 #2、#3、#4 視為 #1 的延伸組件**——可以一次性建立 #1，並在 SKILL.md 內以 sub-section 涵蓋 headless agent / watcher / Windows clipper；若日後使用情境分化再拆。
3. **候選 #5 評估是否合併進 `taiwan-news-classifier`**——以 `mode: daily|weekly` 參數區分，避免兩個 skill description 撞名。
4. **候選 #6、#7 暫不實作**，等下一次 session-harvest 觀察是否重複出現。
