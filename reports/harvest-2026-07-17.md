分析完成。已比對現有 skills 目錄（rivendell + jackskill 共 100+ skills），以下為採集報告。

---

# Session Harvest 報告 — Skill 候選盤點

**採集日期**：2026-07-17
**掃描範圍**：4 個 session／約 168 則訊息
**專案**：`-home-jacktsai`、`-home-jacktsai-claude-workspace`
**工具熱度**：Edit(57)、Bash(50)、Read(24)、Write(6)、TodoWrite(4)、Agent(3)、Skill(2)

## Session 摘要

| # | 專案 | 訊息 | 核心活動 | 觸發的 skill |
|---|------|------|----------|-------------|
| 1 | claude-workspace | 140 | 接手 codex 未完成的工作 → 修 Docker/依賴（`Dockerfile`、`.dockerignore`、`ai_deps.py`），完成 `REDTEAM-REVIEW.json` | project-redteam-review |
| 2 | claude-workspace | 5 | 詢問「這個 folder 有無 skill／算不算全域環境」— 環境定位 | — |
| 3 | jacktsai | 1 | 確認當前對話對應的 WSL 開發路徑 | — |
| 4 | jacktsai | 22 | 建立 claude 工作區＋另建 codex 工作區並安裝 codex CLI；更新 MEMORY | task-brief |

**訊號品質提醒**：4 個 session 中 Session 2/3 屬於一次性瑣事問答、Session 4 屬一次性環境搭建、Session 1 的主流程已由既有 `project-redteam-review` 覆蓋。因此**本批次沒有 Strong 等級候選**，以下為誠實評級。

---

## Skill 候選

### 🟡 Moderate — `codex-handoff`（跨 AI CLI 工作交接）
- **Purpose**：接手另一個 AI CLI 代理（Codex）留下的半成品工作——先重建「已做什麼／停在哪／下一步是什麼」，再往下做。反向也適用（把工作交棒給 codex 前產出交接摘要）。
- **Trigger**：使用者說「把 codex 沒完成的往下做」「接手 codex 的工作」「這是 codex 做到一半的」「交接給 codex」。
- **Category**：workflow
- **Rationale**：此使用者**同時運行 Claude Code 與 Codex CLI 兩個代理**，並刻意用雙工作區把兩者分域（見 `project_workspaces.md`）。Session 1（明確接手 codex）＋ Session 4（搭建雙代理環境）都指向這個反覆出現的協作模式。現有 `context-recovery`（只處理 compaction 後自我復原）與 `dispatching-parallel-agents`（並行 subagent）都不涵蓋「跨異質 CLI 代理的交接」。缺口真實、對此使用者高度個人化。

### 🟡 Moderate — `dual-cli-workspace-bootstrap`（雙 CLI 工作區搭建）
- **Purpose**：在指定根目錄下建立分離的 `claude-workspace` / `codex-workspace`，各自安裝對應 CLI、寫入分域規則，並同步一份 MEMORY 索引記錄佈局。
- **Trigger**：「幫我建一個 claude/codex 工作區」「把跟 claude 有關的都裝進去」「另建 codex 工作區並裝 codex cli」。
- **Category**：meta
- **Rationale**：Session 4 完整走過一次，且已沉澱為記憶（`project_workspaces.md`、`project_skill_layout.md`）。與 jackskill 的 `personal-workspace`（Obsidian 筆記庫）**不重疊**——那個是知識庫，這個是 CLI 代理環境搭建。降為 Moderate 是因為它偏向「一次性搭好就少再用」，重複性待觀察。

### 🔴 Weak — `wsl-session-locator`（工作區／WSL 路徑定位盤查）
- **Purpose**：回答「我這個對話對應到哪個實體資料夾／`\\wsl.localhost\...` 路徑」「這裡算不算全域環境」「這 folder 有沒有 skill」。
- **Trigger**：「這裡對應哪個路徑」「這算全域環境嗎」「這 folder 有 skill 嗎」。
- **Category**：meta
- **Rationale**：Session 2、3 是純實例、且**反覆出現**，但本質是一兩行 `pwd`/`ls` 就能答的瑣事，做成 skill 收益低；且部分能力已被 `env-doctor`、`settings-audit` 涵蓋。**建議不獨立成 skill**，最多在 CLAUDE.md 加一則環境定位備忘。

### 🔴 Weak — `docker-deps-fix`（Docker 依賴收斂修復）
- **Purpose**：讓專案的 Python/AI 依賴在 Docker 內乾淨 build——收斂 `Dockerfile`、補 `.dockerignore`、整理 `ai_deps.py` 之類的依賴清單。
- **Trigger**：「image build 不起來」「幫我修 Dockerfile 依賴」。
- **Category**：backend
- **Rationale**：僅 Session 1 出現一次，且是 redteam-review 的下游收尾動作，重複性未證實。現有 `docker-compose-setup`、`env-doctor` 已部分覆蓋。**暫不建議**，待再出現 1–2 次同型任務再升級。

---

## 建議

1. **值得推進**：`codex-handoff` 是本批次唯一有真實缺口、又貼合此使用者雙代理工作流的候選。建議用 `skill-creator` 起草。
2. **可選**：`dual-cli-workspace-bootstrap` 若未來還要在別台機器重搭環境，值得固化；否則現有記憶檔已夠。
3. **不建議成 skill**：`wsl-session-locator`、`docker-deps-fix`（訊號太弱或已被覆蓋）。

需要我用 `skill-creator` 直接起草 `codex-handoff` 的 SKILL.md 草稿嗎？
