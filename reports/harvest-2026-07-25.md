# Session Harvest 報告

## 一、Session 總覽

| # | 專案 | 訊息數 | 主要工具 | 內容 |
|---|------|--------|----------|------|
| 1, 4 | -home-jacktsai-taiwan-company | 12 / 1 | WebSearch, WebFetch | 台灣公司（三大未來科技，統編 53594157）PE 初步盡職調查 |
| 2, 3 | -home-jacktsai-taiwan-company | 22 / 16 | Bash, Grep, Read, Agent | `/code-review xhigh`，限定「只審 staged diff、只列 CONFIRMED correctness」，兩次幾乎逐字重複相同指令 |
| 5 | -home-jacktsai-taiwan-company | 1 | — | 空白/無內容 |
| 6, 9 | -home-jacktsai | 6 / 7 | Read, Bash | Read-only review：檢視 `skill-lab/jackskill/workflow/collab-dual-brain` 的 UX 改動，明確禁止編輯 |
| 7 | -home-jacktsai | 23 | Bash, Agent, Read | 同一份 read-only 審查，但過程中被使用者中斷 |
| 8 | -home-jacktsai | 1 | — | 空白/無內容 |
| 10–30（共 18 個） | `-tmp-collab-call-*` | 2–5 | StructuredOutput | collab-dual-brain 引擎內部的子代理呼叫：proposal 修訂、獨立驗證者、fresh judge、issue normalizer、red-team 最終稽核 |

**關鍵結構性發現**：30 個 session 中有 18 個（60%）不是「使用者交辦的新任務」，而是既有 `collab-dual-brain`（位於 `~/skill-lab/jackskill/workflow/collab-dual-brain`）在跑自己流程時產生的內部子呼叫（proposal 修訂者、驗證者、裁判、issue 正規化、稽核者五種角色輪替）。另外 3 個是使用者對這套引擎「本身的 UX 改動」做 read-only 複審。也就是說，這批 session 的最大量體其實是**已存在 skill 的正常運作痕跡**，不是待萃取的新模式。

## 二、比對現有 Skill

已確認 `/home/jacktsai/skill-lab/rivendell/skills/` 現況：

- **PE 盡職調查**：`workflow/pe-dd-taiwan`、`workflow/tw-pe-due-diligence` 已存在，但兩者都是**空殼**（`source: harvest-auto`，內容只有 `## TODO`，無實際流程）。Session 1/4 的任務完全落在這兩個 skill 的範疇內，卻沒有可用內容可讀取。
- **公司登記查詢**：`backend/tw-company-lookup`（findbiz.nat.gov.tw 查詢）與 `workflow/customer-intel`（B2B 公司調查）內容完整，已覆蓋 session 1/4 用到的部分能力。
- **Code review（staged-only + xhigh + CONFIRMED-only）**：屬於既有 `/code-review` 指令的參數組合，非獨立流程。
- **collab-dual-brain 本身**：已是成熟既有 skill（`jackskill/workflow/collab-dual-brain`），session 6/7/9 及全部 18 個 `tmp-collab-call-*` 都是在使用/複審它，不構成新模式。

## 三、Skill 候選評估

### 🟡 Moderate

**候選：補完 `pe-dd-taiwan` / `tw-pe-due-diligence` 空殼 skill（建議二選一保留並合併）**
- **用途**：目前有兩個同義但都是空殼的 PE DD skill，實際盡職調查任務（session 1/4）發生時完全沒有內容可依循，等於每次都要臨場重建流程。
- **觸發時機**：使用者要求對台灣公司做 PE/投資初步盡職調查時。
- **類別**：workflow（維護型，非新增）
- **理由**：這不是「發現新模式」而是「既有骨架未填肉」。與其新增第三個 skill，更該做的是：把 session 1 的實際做法（統編查核、WebSearch/WebFetch 公開資訊蒐集、產業與設立資訊比對）填進其中一個，刪除另一個重複空殼，避免未來又長出第三個同名變體。

### 🟢 Weak（不建議獨立成 skill）

**候選：「/code-review staged-only + xhigh + CONFIRMED-only」預設參數**
- **用途**：使用者兩次（session 2、3）都手動貼上幾乎相同的一長串限定條件（只審 staged diff、忽略未 staged、只列 CONFIRMED 或高信心 correctness）。
- **觸發時機**：需要嚴格範圍與嚴格信心門檻的 code review 時。
- **類別**：quality
- **理由**：重複出現顯示這是使用者常用偏好，但這只是**既有 `/code-review` 指令的一組固定參數**，不涉及新工具序列或新領域知識。更輕量的作法是存成一句可貼上的慣用語（或寫進該專案的 CLAUDE.md/常用 prompt 片段），不必新增一個 skill 檔案徒增維護成本。

**候選：collab-dual-brain UX 改動的 read-only 審查 brief**
- **用途**：session 6/7/9 三次幾乎相同地要求「唯讀、不可編輯，只檢視 collab-dual-brain 的未提交 UX 改動」。
- **理由**：頻率夠但範圍極窄（單一專案、單一子目錄），本質就是既有 code-review／project-redteam-review 能力搭配一句固定 scope 限定語，不足以構成獨立、可跨專案重用的 skill。

### 🔴 未發現 Strong 候選

本批 30 個 session 沒有觀察到「重複出現、跨情境可泛化、且尚未被任何既有 skill 覆蓋」的全新工具序列或領域知識。18 個 `tmp-collab-call-*` 雖然工具用量最大（StructuredOutput 佔比最高），但那是既有 `collab-dual-brain` 引擎本身的內部機制，萃取出來只是在重造已存在的輪子。

## 四、建議行動

1. 不新增 skill。
2. 挑一個（建議 `tw-pe-due-diligence`，命名較完整）補上真正的 PE DD 流程內容，刪除另一個空殼，避免重複。
3. 若使用者常用「staged-only + xhigh + CONFIRMED-only」的 code review 組合，建議存成該專案內的慣用 prompt 片段即可，不必立 skill。
