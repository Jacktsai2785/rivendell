# Session Harvest 報告

## 一、Session 摘要

| 分類 | 涉及 Session | 訊息數 | 主要活動 |
|---|---|---|---|
| TW產業新聞日報彙整 | [1]-[6]（-home-jacktsai-taiwan-company） | 1–5 | 綠色永續／消費生活／前瞻科技三產業，每日新聞清單分類 + 2日摘要生成。Bash 呼叫極少（0–4次），屬於已建置好的自動化 pipeline 輸出 |
| Chrome 插件設計協作 | [7] | 28 | podcast 轉文字插件設計，觸發 task-brief gate → AskUserQuestion 釐清 → 與 codex 協作設計 |
| Collab CLI 設計調整 | [8] | 13 | 承 [7]，用 task-brief + Agent + collab-design 迭代調整雙腦 review 機制 |
| 專案架構紅隊盤點 | [9] | 45 | 用 codex + collab-review skill 檢視 taiwan-company 專案 workflow／重複程式碼／耦合度 |
| collab-design/collab-review 內部子呼叫 | [10]-[30] | 2–3 | 全部是 collab-design／collab-review 既有機制自動產生的匿名子 agent 呼叫：設計提案互審、單一 blocker 定向修訂、匿名交叉判定、finding 獨立驗證、issue normalizer。清一色只用 StructuredOutput 回傳結構化結果 |

**關鍵觀察**：30 個 session 裡有 21 個（[10]-[30]）並非使用者發起的新工作流程，而是 `collab-design`／`collab-review` skill 內部產生的匿名子 agent 呼叫（雙盲提案、交叉評審、issue normalizer、blocker 定向修訂）。這些已經是既有 skill 的正常運作痕跡，不是待萃取的新模式。

已核對 `/home/jacktsai/skill-lab/rivendell/skills/` 目錄（backend/docs/frontend/git/meta/quality/workflow 七類），並確認 `task-brief`、`collab-design`、`collab-review`、`codex` 實際掛載在 `~/.claude/skills/`（依 [[project_skill_layout]] 記憶，三個 skill 來源之一），非 rivendell 本身持有，但功能已存在、正常運作中。

## 二、Skill Candidates

### Weak — TW 產業新聞日報彙整器
- **用途**：依產業別（綠色永續／消費生活／前瞻科技）將原始新聞清單分類、生成每日摘要與2日高頻標題彙整
- **觸發情境**：收到「以下是今日台灣『XX』產業的新聞清單，請分類」
- **類別**：workflow
- **評級理由**：這個模式已經是 `/home/jacktsai/taiwan-company/` 內建的專案級 pipeline（main.py + services + routers，非臨時手動流程），Session [1]-[6] 只是它的日常輸出，不是「使用者手動重複、值得抽象成通用 skill」的行為。若要抽成 skill，需先確認此分類邏輯有沒有要用在 taiwan-company 之外的專案；目前沒有證據支持，暫評 Weak，不建議現在建立。

### Weak — 雙腦協作單一 Blocker 定向修訂
- **用途**：收到獨立評審回報的「唯一 blocker」後，只針對該點做最小幅度修訂，不擴大改動範圍
- **觸發情境**：`You are revising your own design proposal. Independent review found ONE blocker issue...`
- **類別**：workflow / meta
- **評級理由**：這是 [10][11][17][18][21]...等 11 個 session 中重複出現的 prompt 模式，但它是 `collab-design` skill 內部協調器自動組出的子任務，不是使用者手動觸發的獨立流程。功能已被既有 skill 封裝，重複建立只會產生職責重疊。不建議新增。

### 無 Moderate/Strong 候選
本批 session 未發現值得新增為獨立 skill 的模式。原因：
1. 有明確重複工具序列的 21 個 session，全部是 `collab-design`/`collab-review` 的既有內部機制（StructuredOutput 為主），屬於「skill 正在運作」而非「skill 缺口」。
2. 使用者發起的 4 個實質任務（[7][8][9] 及其延伸）都已經正確地走 `task-brief` gate → `collab-design`/`collab-review`/`codex` 的既有路徑，沒有出現「手動拼湊、缺 skill 覆蓋」的痛點。
3. 新聞彙整類 session 是既有專案腳本的日常輸出，不是可泛化的手動工作流程。

## 三、建議
這次沒有需要新建的 skill。唯一值得記錄的組織性落差是：`collab-design`／`collab-review`／`task-brief`／`codex` 實際運作良好但掛在 `~/.claude/skills/`，不在 rivendell 的分類目錄下——如果之後要用 rivendell 作為單一 skill 真相來源，可以考慮把這四個搬進 `rivendell/skills/workflow/` 或 `meta/`，但這是搬遷／整併問題，不是新 skill 需求，建議另外找時間處理而非現在動作。
