# Session Harvest — 2026-05-28

## Session Summary

- **日期範圍**：近期 11 個 sessions（taiwan-company 專案）
- **訊息總數**：~84 messages
- **總成本**：$0.00（cost 為 0，可能為 cache hit 或本機統計缺漏）
- **專案**：`-home-jacktsai-taiwan-company`（單一專案）
- **Top tools**：WebSearch (39) → WebFetch (20) → ToolSearch (9) → Bash (4) → Grep (1)

### 主要活動模式

11 個 sessions 全部圍繞「**台灣未上市/新創公司之 PE 視角研究**」展開，可拆成三類動作：

| 動作群 | 對應 sessions | 既有 skill 覆蓋 |
| --- | --- | --- |
| 產業別分類（JSON 輸出） | #1, #3 | ✅ `taiwan-industry-classifier` |
| 公司官網查找（單 URL 輸出） | #2, #7 | ✅ `tw-company-website-finder` |
| 初步 PE DD 備忘錄 | #8, #9 | ✅ `tw-company-pe-memo` |
| **深度補充搜尋 + 修訂備忘錄** | #4, #5, #6, #10, #11 | ❌ **未覆蓋** |

→ 沒有覆蓋的「第二輪深度補充」是這次 harvest 的核心發現。

---

## 已存在 skill 對應檢查

掃描 `skills/workflow/` 已確認以下相關 skill 已存在，**不重複建立**：

- `tw-company-pe-memo` — 初版 DD 備忘錄
- `tw-company-website-finder` — 官網查找
- `tw-company-dd-pipeline` — 批次管線（含官網 + PE DD）
- `taiwan-industry-classifier` — 產業分類
- `tw-company-name-extractor` — 公司名擷取
- `tw-company-lookup`（backend）— findbiz 深度查詢

---

## Skill 候選清單

### [Strong] `tw-company-pe-memo-refine` — PE 備忘錄第二輪深度修訂

- **目的**：對 `tw-company-pe-memo` 產出的初版備忘錄，執行第二輪「補充搜尋 → 證據抓取 → 修訂」迴圈，加入媒體報導、獲獎入選、創辦人背景、競品分析等深度訊息。
- **觸發**：使用者貼上既有 PE 備忘錄並說「深度補充搜尋並修訂備忘錄」、「補強這份 memo」、「加入媒體/報導內容」、「revise memo」、「v2 of memo」。
- **分類**：`workflow`
- **Rationale**：
  - Sessions #4, #5, #6, #10, #11 **5 個 sessions** 使用完全相同的 prompt 模板：「步驟 1：用 WebSearch 搜尋『公司名 報導 OR 新聞 OR 採訪 OR 入選 OR 獲獎 OR 媒體 OR 創業』」
  - 是 `tw-company-pe-memo` 之後的後續迴圈，不是同一個動作
  - 工具用量集中在 WebSearch (2–9 次/session) + WebFetch (0–6 次/session)，模式高度一致
  - 目前每次使用者要手動貼整套 prompt 模板，封裝成 skill 可顯著降低 token + 確保模板一致
- **與既有 skill 關係**：可在 `tw-company-pe-memo` SKILL.md 加 cross-link，建議作為「v1 完成後的第二步」

### [Moderate] `tw-company-news-evidence-search` — 公司媒體曝光證據檢索

- **目的**：給定公司名，搜尋並彙整其媒體曝光（報導、入選、獲獎、創辦人專訪），輸出結構化證據清單（含 URL + 摘要 + 日期）。
- **觸發**：「查 X 公司的媒體報導」、「有沒有得獎紀錄」、「創辦人專訪」、「media coverage」、「X 入選 OR 獲獎」。
- **分類**：`workflow`
- **Rationale**：
  - Sessions #4, #5, #6, #10, #11 **皆有獨立執行此一子步驟**（步驟 1 的 WebSearch 模板）
  - 可作為 `tw-company-pe-memo-refine` 的內部步驟，也可獨立使用（例如：寫提案前快速查公司媒體聲量）
  - 但因為單獨價值較窄、且大部分時候會跟 refine 一起出現，列為 Moderate
- **若 refine skill 已封裝此邏輯**：此 skill 可降為 Weak 或合併

### [Weak] `pe-memo-iteration-tracker` — 備忘錄版本迭代追蹤

- **目的**：替每家公司維護 memo 的 v1/v2/v3 版本紀錄，標記每次修訂加入的新資訊來源。
- **觸發**：「memo 第二版」、「比較 v1 v2」、「memo 改了什麼」。
- **分類**：`workflow` 或 `docs`
- **Rationale**：
  - 從 sessions 觀察到使用者會做 memo 多輪修訂，但目前**沒有**版本管理機制
  - 列為 Weak 因為：(1) 樣本只有 5 個 sessions 出現修訂行為、(2) 用 git/檔名後綴就能滿足、(3) 跟 `tw-company-pe-memo-refine` 重疊
  - 建議**不要單獨建**，併入 `tw-company-pe-memo-refine` 的輸出規範即可

---

## 觀察到的工作流程缺口（非 skill 建議）

1. **batch + refine 結合**：`tw-company-dd-pipeline` 目前只覆蓋 v1（官網 + 初版 memo），缺一個 batch refine 入口。如果 refine 變高頻使用，可考慮擴充 pipeline 加 v2 階段。
2. **WebSearch query 模板沉澱**：「公司名 報導 OR 新聞 OR 採訪 OR 入選 OR 獲獎 OR 媒體 OR 創業」是反覆出現的 query 模板，值得抽到 skill 內成為固定段落（避免每次靠記憶手打）。

---

## 建議下一步

1. **建立 `tw-company-pe-memo-refine`**（Strong 候選）— 這是這次 harvest 唯一一個**確定值得建**的新 skill。
2. **暫不建** `tw-company-news-evidence-search` 與 `pe-memo-iteration-tracker`，等再累積幾個 session 證明獨立價值再評估。
3. **回頭補強** `tw-company-pe-memo` 與 `tw-company-dd-pipeline` 兩支 skill 的 SKILL.md（目前皆為 auto-generated stub，內容只有 TODO），這次 harvest 樣本足夠補實際 workflow 步驟。
