# Session Harvest — 2026-05-31

## Session Summary

- **日期範圍**：30 個近期 sessions（全部來自 `-home-jacktsai-taiwan-company` 專案）
- **訊息總數**：約 426 則
- **主要工具使用**：WebSearch (264) / WebFetch (105) / ToolSearch (23) / Skill (2)
- **核心任務**：對台灣公司逐家進行 PE 視角的初步盡職調查（DD），輸出結構化投資備忘錄
- **涉及公司**：格雷迪奧、豐溢綠能材料、真實引擎、康統醫學科技、邦易聽、夆泩數位發展、宏岳國際、補完計畫、品辰科技物流、可立可、活優科技、呼叫網紅、大兼廣告、極山、豆豆方、寶翔能源、大橡科技、鈞泰運動數位科技、震豪數位科技、安寶角鋼、塞特知識、堆疊、維特魯威運動科技（共 23 家不同公司，宏岳國際出現 7 次但全部失敗）

## 主要觀察

### 1. 既有 skill 觸發率極低（重大）

`tw-company-pe-memo` skill **已經存在**（`skills/workflow/tw-company-pe-memo/SKILL.md`），且 30 個 sessions 的 intent 字串幾乎完全一致（同一個 PE DD prompt 模板）。但 **只有 2 個 sessions 實際觸發到 Skill 工具**（sessions [17] 和 [27]），其餘 26 個（不含失敗的 7 個 stub）都是直接走 WebSearch + WebFetch 而沒有走 skill workflow。

**根因推測**：該 skill 的 `description` 是 auto-generated 的 placeholder，`TRIGGER when: when working with tw-company-pe-memo` 是無效的觸發語，無法被 router 比對到 PE DD prompt 模板的關鍵詞（「資深私募股權」「盡職調查」「投資備忘錄」「統一編號」等）。

### 2. 宏岳國際持續失敗（7/30）

Sessions [8]、[19]、[20]、[25]、[26]、[28]、[29] 都是「宏岳國際有限公司，統編 54105706」，每次都只跑了 1 則訊息（無任何工具呼叫）就終止。這代表有一個外部 orchestrator（可能是 batch DD pipeline）在重複丟同樣的 prompt 但每次都卡死或失敗，沒有 fallback / 退避機制。

### 3. 純 web research 沒接到結構化資料源

每個 session 平均 11 次 WebSearch + 5 次 WebFetch，卻完全沒用到本機已建構的 MOPS 資料庫、`tw-company-identify`、`tw-company-lookup` 等結構化資料工具。代表 DD 流程沒有把「先抓 API/DB 結構化資料 → 再 web 補足」的 SOP 內化。

## Skill Candidates

### Strong（高優先）

#### A. `tw-company-pe-memo-trigger-fix`（meta / 修補既有 skill）
- **purpose**：把現有 `tw-company-pe-memo` 的 placeholder description 改寫成真正可被 router 偵測的 trigger 字串。
- **trigger 範例**：prompt 中同時出現「資深私募股權」+「盡職調查」+「統一編號」、或包含「【基本資料】」「【投資要點】」格式段落、或使用 `tw-company-pe-memo` 的標準模板開頭。
- **category**：meta（修補既有 skill 而不是新增）
- **rationale**：30 個 sessions 中有 28 個應該觸發既有 skill 卻沒觸發，這是 ROI 最高的單一改動。應該直接 edit 既有 SKILL.md 的 frontmatter，不需要新 skill。

#### B. `tw-company-dd-orchestrator-guard`
- **purpose**：偵測 batch DD pipeline 在同一家公司連續失敗（如 1 則訊息就退出 → 視為 stub failure），自動 quarantine 該公司、寫入 `failed-dd-targets.log`、停止對該統編的重試。
- **trigger**：當 orchestrator 看到同一統編出現 N≥3 次 stub session 時。
- **category**：workflow
- **rationale**：宏岳國際 7 次失敗是浪費資源的明顯 anti-pattern，需要 circuit breaker。可能可以併入既有的 `tw-company-batch-dd-orchestrator` skill。

### Moderate

#### C. `pe-dd-structured-source-first`
- **purpose**：規範 PE DD 流程的「結構化資料優先」順序：先 `tw-company-identify` → `tw-company-lookup` → MOPS DB 查詢（被投資揭露 / 月營收 / 損益）→ 才補 WebSearch / WebFetch。每個 session 至少省 5-8 次 WebSearch，並把財務數字錨定在官方資料而不是新聞稿。
- **trigger**：使用者提供統編、要求做 DD 或投資備忘錄。
- **category**：workflow
- **rationale**：30 個 sessions 平均花 16 次 web 呼叫卻沒查 MOPS，是明顯的「沒用對工具」。但這可能已隱含在 `pe-memo-deep-research` 中，需先 audit。

### Weak

#### D. `web-research-budget-tracker`
- **purpose**：在 WebSearch + WebFetch 呼叫累計超過 N 次時，提示使用者「這個 session 已花了 X 次 web 呼叫，是否該收斂結論」。
- **rationale**：觀察到部分 sessions 達 18-19 次 WebSearch，邊際資訊報酬遞減。但這比較是 telemetry 層的事，未必需要獨立 skill；可能用 `agent-observability` 處理較合適。

## 建議下一步行動

1. **立即**：edit `skills/workflow/tw-company-pe-memo/SKILL.md` frontmatter，把 description / TRIGGER when 改寫成可實際 match 的字串。完成後 30 個 sessions 中的 28 個都會走正確流程。
2. **本週**：audit `tw-company-batch-dd-orchestrator` 是否有 circuit breaker；若無則加上對 stub session（1 message 就終止）的偵測。
3. **觀察**：在改完 trigger 後再跑一輪 harvest，看實際觸發率是否從 ~7% 升到 >80%。
