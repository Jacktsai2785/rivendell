Harvest 報告已寫入 `reports/harvest-2026-05-14-batch3.md`。簡要結論：

## 摘要

30 個 sessions ≈ 425 messages，3 個專案，主題分佈：
- **PE DD memo**：17 sessions（已有 `tw-company-pe-memo`，建議檢視為何沒自動觸發）
- **文件差異分析（含 PDF OCR）**：7 sessions
- **Port 規劃 / 服務重啟**：3 sessions
- **其他**：patent scraper、外部 skill 安裝、新聞彙整、資料夾清理

## Skill 候選

| 候選 | 評級 | 理由 |
|---|---|---|
| **`pdf-ocr-routing`** | Strong | Sessions #1–6 全部卡在「empty diff JSON」silent fail，#27 user feedback 直指根因 — 沒做 PDF 類型自動偵測。既有 `file-diff-agent` 是 scaffolding，未覆蓋；`office-pdf` 是通用 SDK 無啟發式 |
| **`multi-project-port-replan`** | Strong | Session #28（94 msgs，當週最大）的核心需求。`dev-port-conflict-fix` 只處理單一衝突；缺「全機盤點 + 分段制重新規劃」流程 |
| **`vscode-wsl-port-forwarding-debug`** | Moderate | 只 1 session 觸發，但 WSL 環境會持續遇到。建議第 2 次出現再升格 |
| **`github-skill-installer`** | Moderate | 與 `gdrive-to-skills` 對稱，但目前僅 1 session 觸發 |
| **`scraper-ground-truth-verifier`** | Weak | 改寫入 `self-improving-agent` 範例即可，不獨立 |

## 建議下一步

1. 建立 `pdf-ocr-routing`（跨 7 sessions）
2. 建立 `multi-project-port-replan`（當週最大 session）
3. 延伸 `file-diff-agent` SKILL.md 加入「empty diff JSON challenge」段落
4. 檢視 `tw-company-pe-memo` 為何 17 個 sessions 都從 raw prompt 而非 `/skill` 啟動
