已核對現有 skills（rivendell + jackskill + 全域已安裝清單）與過去 4 份 harvest 報告。這批 30 個 session 幾乎都不是「新模式」——多數是既有 skill 的正常運作紀錄，真正值得留意的訊號其實是「上次已標記過、這次又出現、還是沒人做」。以下是報告（依 rivendell 專案規則，`reports/` 由排程 agent 自行維護，這裡先以對話輸出呈現，不寫檔）：

---

# Session Harvest 報告（2026-07-24）

## 一、Session 總覽

| # | 專案 | 訊息數 | 主要活動 |
|---|------|-------|---------|
| 1, 3, 4 | -home-jacktsai-taiwan-company | 各 1 | 台灣「前瞻科技／綠色永續／消費生活」產業新聞清單分類，headless 單訊息、0 工具呼叫 |
| 2 | -home-jacktsai-taiwan-company | 1 | 資深 PE 投資人視角對「三大未來股份有限公司」做初步盡職調查，純文字推理、0 工具 |
| 5 | -home-jacktsai-taiwan-company | 1 | WebSearch 查公司官方網站，嚴格限定「只回 URL」輸出格式 |
| 6 | -home-jacktsai | 10 | 比對 taiwan-company 本地專案與 repo 版本差異，Bash × 9 |
| 7–30（24 個） | -tmp-collab-call-* | 各 1–3 | `collab-dual-brain` skill 內部的 proposer / reviewer / synthesizer / fresh-context judge / reviser / issue-normalizer 呼叫，全部搭配 StructuredOutput |

**跨 session 觀察：**

1. **24/30 session 是 `collab-dual-brain` 的內部子呼叫**——proposer 出設計 → reviewer 匿名審查 → judge 對僵局裁決 → synthesizer 彙整 → reviser 針對 blocker 修訂，是完整的雙盲協作管線在正常運作，且 `collab-dual-brain` 已存在（`jackskill/workflow/collab-dual-brain`）。這不是新模式，是既有 skill 被大量使用的證據。
2. **taiwan-company 產業新聞分類（session 1, 3, 4）與上次（2026-07-22）報告的模式完全相同**：輸入格式一模一樣（「以下是今日台灣『X產業』的新聞清單…請分類」），這是排程化 headless 管線的第二次出現，規則仍未固化成 skill。
3. **PE 盡職調查（session 2）與官網查找（session 5）**都是 0 工具呼叫的純文字/單次搜尋任務，屬於 taiwan-company 專案自己 pipeline 產生的 prompt，不是 Claude Code 互動式工作流程。

## 二、Skill 候選清單

### 🟢 Strong：`tw-industry-news-classify`
- **用途**：接收台灣媒體新聞清單（依產業別：消費生活、前瞻科技、綠色永續…），套用固定分類規則與輸出格式，供每日排程 headless agent 使用。
- **觸發時機**：headless 輸入「以下是今日台灣『X產業』的新聞清單…請分類」；或使用者說「新聞分類」「產業新聞整理」。
- **分類建議**：workflow
- **理由**：**這是同一模式第二次被 harvest 標記為 Strong**（上次：2026-07-22 報告），這次又出現 3 個結構完全相同的 session，代表管線每天都在跑，規則卻仍只活在 prompt 裡，格式漂移風險持續累積。優先度應提高，不建議再拖。
- **現有相似**：`keyword-discovery`、`tender-scraper` 皆不涵蓋新聞分類；無重複。

### 🟡 Moderate：補完 `pe-dd-taiwan` / `tw-pe-due-diligence`
- **用途**：這兩個 skill 已存在，但內容都是 `source: harvest-auto` 產生的空殼（只有 TODO，無實際 checklist / 資料來源 / 輸出格式）。Session 2 的「PE 投資人初步盡調」正是這兩個 skill 該涵蓋的具體案例，可作為填充內容的素材。
- **觸發時機**：既有 frontmatter 已定義。
- **分類建議**：workflow
- **理由**：不建議新開 skill——兩個名稱高度重疊（`pe-dd-taiwan` vs `tw-pe-due-diligence`），應先決定合併成一個再補內容，而不是留著兩個空殼加深混淆。
- **現有相似**：`customer-intel`、`investment-research`、`tw-company-lookup` 可作為資料來源串接對象。

### 🔴 Weak：`official-website-finder`（不建議）
- **原因**：session 5 只是一次 WebSearch + 嚴格輸出格式限制，屬於 taiwan-company 專案自己 pipeline 裡的 prompt 模板，複雜度不足以立 skill，Claude 已能穩定處理這類單步驟任務。

### 🔴 Weak：`local-vs-repo-drift-check`（不建議）
- **原因**：session 6 用 9 次 Bash 做本地專案與 repo 版本比對，屬於一次性、專案特定的檢查（且很可能只是 `git diff`/`git log` 的組合），沒有需要固化的領域知識。

### 🔴 Weak：collab-dual-brain 內部呼叫模式（不建議）
- **原因**：24 個 session 都是既有 `collab-dual-brain` skill 的正常子呼叫，不是新模式，是該 skill 運作良好的證據，不需另立 skill。

## 三、建議下一步

1. **優先建立 `tw-industry-news-classify`**——這是唯一連續兩次 harvest 都命中的 Strong 候選，管線每天都在跑，拖越久格式漂移成本越高。要建立的話我可以用 `skill-creator` 起草。
2. **`pe-dd-taiwan` 與 `tw-pe-due-diligence` 建議先合併再補內容**，不要維持兩個空殼並存——要我先幫你比對兩者 frontmatter 差異、提出合併方案嗎？
3. 其餘候選（官網查找、專案版本比對、collab-dual-brain 內部呼叫）都不建議立新 skill。
