報告已寫入 `reports/harvest-2026-06-01-sessions.md`。

**核心發現**：30 個 session 中 **約 33%（10 個）是 silent-fail 樣態**（msg=1, tools=0），其餘 22 個是同一個 PE DD prompt 模板。重複目標公司（玩艸植造 ×6、律果科技 ×6）多半是 `pe-memo-silent-fail-recovery` 的自動 retry 結果。

**Skill 建議**（依強度排序）：

| 強度 | 名稱 | 目的 |
|---|---|---|
| Strong | `pe-memo-silent-fail-rate-alert` | 把 silent-fail 比例做成可觀測告警，不只靠事後 retry |
| Moderate | `pe-memo-already-generated-guard` | session [28] 正在開發的 UX 抽成 skill：dispatch 前先查 memo 是否已存在 |
| Weak | `single-fact-extractor-prompt` | session [21] 的「只輸出單欄、禁說明」prompt 樣式，建議併入文件不獨立 |
| Weak | `pe-memo-materials-only-mode` | session [15] 的「禁網路、只看附檔」變體，樣本太少先觀察 |

**反向 takeaway**：相關 skill 已有 15+ 個，繼續細分會增加 routing 混亂；33% silent-fail 比例本身值得用 `investigate` 追根因，比再加 skill 更有 leverage。
