現有 harvest 文件確認完畢。今天已有一份 sessions 報告存在（涵蓋 jk-nb/mops-dbs/taiwan-company），這份 digest 是另一批（TWstock-invest + mops-dbs-mops-notes）。直接輸出報告：

---

## Session Harvest Report — 2026-06-09 (Batch 2)

> 分析期間：本 digest 30 場 sessions｜訊息總量：~169｜總費用：$0.00
> 專案：`-home-jacktsai-TWstock-invest`、`-home-jacktsai-mops-dbs-mops-notes`

---

### Session 總覽

| # | 專案 | 訊息數 | 主題摘要 |
|---|------|--------|----------|
| 1 | TWstock-invest | 140 | 前端「資料不足」除錯 → IndicatorTable.tsx 顯示邏輯修正；股票類別查詢（7810 是否金融保險股） |
| 2–30 | mops-dbs-mops-notes | 1×29 | 台灣上市公司合併財報附註結構化抽取（完全相同的 headless 單次呼叫，批次作業，各 1 msg，無互動） |

---

### Skill 候選名單

#### 🔴 Strong

---

**`mops-notes-structured-extractor`**

| 欄位 | 內容 |
|------|------|
| **目的** | 封裝台灣上市公司合併財報附註 OCR 文字 → 結構化 JSON 的提示模板、角色設定與 OCR 雜訊清理規則 |
| **觸發語** | "從財報附註抽結構化資料"、"把這段附註轉成 JSON"、"OCR 附註要怎麼解析"、"抽 section/item/amount" |
| **分類** | `workflow` |
| **不重複的 skill** | `mops-financial-scraper`（爬蟲入庫）、`mops-individual-financial-report`（個體財報建構）、`mops-filer-list-reconcile`（申報清單核對）均不含附註欄位抽取邏輯 |

**理由**：  
Sessions 2–30 是 **29 個完全相同的 1-msg headless 呼叫**，提示內容固定不變：

- **System prompt 角色**：「你是台灣上市公司合併財報附註結構化抽取器」
- **輸入**：財報附註段落文字（含 OCR 雜訊、空白錯位）
- **輸出格式**：`[{"section": "...", "item": "...", "amount": ...}]` JSON 陣列

29 次相同呼叫 = 最強的重複頻率信號，每次手動執行代表使用者在重複貼入同一套提示，目前沒有任何 skill 封裝此提示模板。OCR 雜訊容錯（空白錯位、標點異常）屬於非顯見領域知識，值得記錄在 skill 中。

---

#### 🟡 Moderate

---

**`mops-notes-batch-headless-pipeline`**

| 欄位 | 內容 |
|------|------|
| **目的** | 自動化批次呼叫附註抽取（取代 29 次手動 1-msg session），串接 PDF 頁面清單 → 附註 OCR 文字 → JSON 入庫 |
| **觸發語** | "把這批公司的附註一次跑完"、"排程附註抽取"、"headless 批次跑 mops_notes" |
| **分類** | `workflow` |
| **依賴** | 建在 `mops-notes-structured-extractor` 提示模板之上 |

**理由**：  
29 次手動執行是尚未自動化的直接證據，符合 `headless-agent` 使用場景。但 1-msg sessions 本身沒有展示批次協調的工具使用（無 Bash/Monitor/ScheduleWakeup），批次結構細節需從相關 session（如今天 sessions.md 中的 session 18）補充，故評為 Moderate。

---

#### ⚪ Weak（已有對應 skill）

---

**Session 1（TWstock-invest 前端除錯）→ 不建立新 skill**

`stock-data-gap-diagnose`（`skills/workflow/stock-data-gap-diagnose/SKILL.md`）已作為 stub 存在，目前 session 正是這個 skill 的核心使用場景（前端顯示「資料不足」→ 系統化根因診斷）。

**建議**：補完現有 stub 的實作細節（診斷流程、Bash 查詢指令、常見根因清單），而非建立新 skill。

---

### 重點觀察

| 觀察 | 含義 |
|------|------|
| 29 次相同 headless prompt | 附註抽取尚無 skill，且已在批次生產使用中 |
| 所有 mops-notes sessions 均 0 工具使用 | 純 LLM 推理任務，提示模板是唯一可複用的資產 |
| Session 1 未觸發任何 skill | `stock-data-gap-diagnose` stub 存在但未被觸發，可能缺少 trigger 關鍵字 |
| TWstock-invest 與 mops-notes 是兩個獨立專案 | 附註抽取技能應設計為跨專案通用（不綁定 mops-dbs） |

---

**建議優先行動**：先用 `/skill-creator` 建立 `mops-notes-structured-extractor`（封裝提示模板 + JSON schema），再評估是否要把 `mops-notes-batch-headless-pipeline` 獨立出來或合併入現有的 `mops-notes-backfill-monitor`。
