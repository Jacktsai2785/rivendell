# Session Harvest Report — 2026-05-10

> 自動分析 4 個 Claude Code session，識別可複用的工作流程模式與 skill 候選項。

---

## Session 摘要

| # | 專案 | 訊息數 | 主要活動 | 關鍵檔案 |
|---|------|--------|----------|----------|
| 1 | taiwan-company | 12 | 偉康科技 PE 初步盡調（DD）研究，使用 WebSearch + WebFetch 蒐集公開資料 | — |
| 2 | mops_investee | 137 | 修復前端「查找公發公司持股」查無結果的 bug；財務資產類別顯示正規化；無持股比例時改秀持股數量 | app.js、c1f2e3d4a5b6 migration、cli.py、companies.py |
| 3 | rivendell | 65 | server.py 開發（細節有限，大量 Bash + Edit） | server.py |
| 4 | mops_investee | 294 | 確認系統僅抓合併財報；新增個體財報（非合併）爬取支援；FVOCI 分類辨識；schema 擴充與 migration | pdf_individual.py、filing.py、mops_index.py |

**工具使用分布**：Bash 主導（331 次），顯示以本地開發/除錯為主；Edit（59）+ Read（41）為標準代碼修改流程；TodoWrite（13）顯示有規劃性工作。

---

## Skill 候選分析

### ✅ Strong — mops-individual-financial-report

**目的**：為現有 MOPS 財務爬蟲增加個體財報（非合併）抓取支援

**觸發詞**：「抓個體財報」「非合併財報」「個別財報 PDF」「pdf_individual」「個體 vs 合併」「需要公司本體的持股明細」

**類別**：`workflow/`（或擴充現有 `mops-financial-scraper`）

**理由**：
- Session 4（294 則）的核心工程工作：系統原本只有合併財報，需要新建 `pdf_individual.py`、更新 `filing.py` 的分派邏輯、更新 `mops_index.py` 的查詢條件、新增 migration。
- 這套工作流程有清楚的重複模式：①確認現有 schema 是否有 `report_type` 欄位 → ②找 MOPS 個體財報 PDF 下載端點 → ③實作分離的 PDF parser → ④更新 filing dispatcher → ⑤跑 migration。
- 與現有 `mops-financial-scraper` 的差異：後者描述為「歷史財務三表、月營收、季/年度**合併**營收」，明確未覆蓋個體財報；兩者可互補，但個體財報有獨立的 MOPS 端點結構與 PDF 格式。
- 在 Session 2 的前端 bug 也因為缺少個體財報資料而衍生問題，說明這是基礎建設層次的需求。

**工作流程草稿**：
```
1. 確認 MOPS 個體財報端點（與合併財報的 URL param 差異）
2. 建立 pdf_individual.py：header 識別 → 表格解析 → category 對應
3. 更新 filing.py：依 report_type 分派到對應 parser
4. 擴充 schema：report_type enum + 個體專用欄位
5. 驗證：以已知公司（原相、偉康）交叉比對個體 vs 合併數字
```

---

### ✅ Strong — taiwan-financial-category-display

**目的**：將 MOPS/XBRL 原始財務資產類別代碼正規化為台灣 GAAP/IFRS 標準顯示名稱，並提供持股比例 → 持股數量的 fallback 顯示邏輯

**觸發詞**：「財報資產類別怎麼顯示」「FVOCI 是什麼」「持股比例沒資料時怎麼辦」「財務資產分類顯示錯誤」「顯示成其他長期股權但應該是...」

**類別**：`workflow/`（或 `backend/`）

**理由**：
- Session 2 的核心問題之一：前端把「透過其他綜合損益按公允價值衡量之金融資產（FVOCI）」顯示成通用的「其他長期股權」，需要對應到財報上的正確類別名稱。
- Session 4 中同樣遇到 FVOCI 分類辨識問題（偉康科技在原相個體財報中的持股即為 FVOCI 類別）。
- 這是**純粹的領域知識**（domain knowledge）：台灣 IFRS 下的金融資產四大分類（FVTPL、FVOCI、AC/HTM、權益法）在 MOPS 資料中常以縮寫或代碼儲存，前端/報表需要展示完整名稱。
- Fallback 邏輯（持股比例 null → 改顯示持股數量，如「259千股（259張）」）是通用需求，任何顯示台灣公司持股資料的系統都需要。
- 可編碼為：類別對照表（code → 中文名稱）+ fallback display function。

**知識核心**（應編入 skill）：
```
FVTPL → 透過損益按公允價值衡量之金融資產
FVOCI → 透過其他綜合損益按公允價值衡量之金融資產  
AC    → 按攤銷後成本衡量之金融資產
HTM   → 持有至到期日金融資產（舊準則）
equity_method → 採用權益法之投資
cost_method   → 成本法投資（非上市）
```

---

### 🟡 Moderate — mops-investee-holdings-debug

**目的**：當「查找公發公司持股」功能回傳空結果或錯誤結果時，系統化排查 MV → 資料管線 → 前端查詢的整條鏈路

**觸發詞**：「查無公發公司揭露持有此公司股份」「investee holdings 找不到」「持股查詢沒結果但資料應該有」「MV 沒有更新」

**類別**：`workflow/`

**理由**：
- Session 2 的主要 debug 流程，歷時超過 100 則對話，涉及 migration（c1f2e3d4a5b6_fix_mv_raw_name_groupby）+ app.js 前端查詢邏輯 + companies.py 後端 API。
- 排查步驟有明確模式：①確認 MV 資料是否存在（直接 SQL 查詢）→ ②確認 groupBy 邏輯（公司名稱正規化）→ ③確認前端傳入的 company_name 是否與 MV 的 key 一致 → ④確認 API endpoint 回傳格式。
- 弱點：此 skill 較依賴 `mops_investee` 專案結構，通用性有限。升格為 Strong 的條件是若未來其他專案也出現類似的 MV + 前端持股查詢鏈路。

**排查流程草稿**：
```
1. 直接 SQL：SELECT * FROM v_investee_holdings WHERE company LIKE '%目標公司%' LIMIT 5
2. 確認 MV 最後 refresh 時間
3. 確認 groupBy key 的正規化方式（full name vs. short name）
4. 確認前端傳入的 query param 與 MV key 的格式一致
5. 確認 API 的 company_name 比對是 exact match 還是 LIKE
```

---

### ⬜ Weak — server-py-fastapi-debug（Session 3）

**理由**：Session 3 僅有 65 則，intent 截斷（`[Request interrupted by user for tool use]`），資訊不足以識別可複用的模式。大量 Bash + Edit 作業在 server.py，但無法判斷是新功能開發、bug fix 還是 refactor。**建議跳過**，等後續 session 補充更多 context。

---

## 與現有 Skills 的重疊分析

| 候選 Skill | 最近似的現有 Skill | 差異說明 |
|---|---|---|
| mops-individual-financial-report | `mops-financial-scraper` | 現有 skill 明確描述為合併財報；個體財報有不同端點、PDF 格式、schema 需求 |
| taiwan-financial-category-display | `mops-financial-scraper`、`mops-investee-cross-ref` | 現有 skills 未涵蓋 IFRS 類別映射與 fallback 顯示邏輯；此為純顯示層知識 |
| mops-investee-holdings-debug | `mops-investee-cross-ref` | 現有 skill 描述查詢流程；此 skill 聚焦在「查詢失敗時怎麼排查」的 debug 路徑 |

---

## 建議行動

| 優先級 | 行動 |
|--------|------|
| P1 | 新建 `skills/workflow/mops-individual-financial-report/SKILL.md` |
| P1 | 新建 `skills/workflow/taiwan-financial-category-display/SKILL.md` |
| P2 | 考慮將排查步驟補入 `mops-investee-cross-ref/SKILL.md`（而非新建 skill） |
| P3 | 等待更多 Session 3 context 再決定是否建立 server.py 相關 skill |

---

*Generated by session-harvest · 2026-05-10*
