# Session Harvest Report — 2026-03-16

## 會議總覽

| # | 專案 | 日期 | 訊息數(估) | 主要活動 |
|---|------|------|-----------|---------|
| 1 | Projects (root) + sales-assistant | 2026-03-16 | ~200 | 客戶情蒐（穩德文具印刷）、補貼資料匯入、跨專案報告搬遷 |
| 2 | news-stock | 2026-03-16 | ~85 | 投資組合每日健檢（Continuous Mode）、宏觀日曆 + 多標的 WebSearch |
| 3 | sales-assistant | 2026-03-16 | ~400 | 檔案上傳修復、知識庫頁面全端開發、行事曆時間軸 UI 修正 |

**共計：3 sessions / ~685 messages / 3 projects**

---

## 觀察到的工作模式

### 1. 跨專案報告搬遷與整合
**Session 1** 中，使用者發現 `reports/customer-intel/` 放在錯誤的目錄下，觸發了一個多步驟搬遷流程：
- 識別正確歸屬的 repo → `cp -r` 複製 → 更新 `CLAUDE.md` 參照 → 修改 skill 中的路徑 → 建立 symlink 保持相容 → 清理備份 → commit

### 2. 全端功能建立（Next.js + FastAPI + Nexus）
**Session 3** 中，建立知識庫頁面的流程：
- 後端：FastAPI router 新增 endpoint → service layer 函式
- 前端：nexus-api.ts 新增 client method → 建立 page.tsx → 更新 sidebar nav
- 驗證：`tsc --noEmit` → `curl` 測試 → 瀏覽器確認

### 3. 投資組合每日監控
**Session 2** 中，investment-research skill 的 Continuous Mode 執行良好：
- 讀取 portfolio-state.json → 逐一 WebSearch 持倉 → 查詢宏觀日曆 → 產出 daily dashboard → 更新 state

### 4. 客戶情蒐 → Nexus 同步
**Session 1** 中 customer-intel skill 完整執行：
- WebSearch → 公司網站爬取 → 撰寫報告 → create_client API → 更新 INDEX.md → tag 關聯

---

## Skill 候選評估

### Strong — 無

> 這三個 session 主要是已有 skill 的實際運用（customer-intel、investment-research），以及專案特定的開發工作。沒有發現足夠泛化且重複出現的全新模式。

### Moderate

#### 1. `nexus-fullstack-feature`
| 欄位 | 內容 |
|------|------|
| **名稱** | nexus-fullstack-feature |
| **用途** | 在 sales-assistant 專案中快速建立全端功能：FastAPI endpoint + Nexus service layer + Next.js page + sidebar 整合 |
| **觸發** | 使用者要求在 sales-assistant 新增頁面或功能時 |
| **分類** | workflow |
| **理由** | Session 3 中知識庫頁面的建立是一個清晰的 7 步驟流程，且 sales-assistant 持續開發中，未來新功能（如報表頁、設定頁）會重複此模式。但目前僅出現一次，且高度綁定 sales-assistant 的技術架構（FastAPI + Next.js + Nexus API），泛化程度有限。 |
| **評等理由** | 單次出現 + 專案專屬 → Moderate。若在後續 session 再出現類似模式則升級為 Strong。 |

### Weak

#### 2. `cross-project-migrate`
| 欄位 | 內容 |
|------|------|
| **名稱** | cross-project-migrate |
| **用途** | 跨專案搬遷檔案/目錄：複製 → 更新引用路徑 → 建立 symlink → 清理 |
| **觸發** | 使用者要求把檔案從一個 repo 搬到另一個 |
| **分類** | workflow |
| **理由** | Session 1 中搬遷 reports/customer-intel 的流程繁瑣且容易出錯（漏改路徑、symlink 方向搞反）。但此類操作本質上是一次性的，且每次搬遷的檔案結構和引用方式都不同，難以模板化。 |
| **評等理由** | 一次性操作 + 難以泛化 → Weak |

#### 3. `nexus-client-enrichment`
| 欄位 | 內容 |
|------|------|
| **名稱** | nexus-client-enrichment |
| **用途** | 批次更新 Nexus 客戶資料欄位（行業分類、標籤等） |
| **觸發** | 使用者要求補齊客戶 metadata |
| **分類** | workflow |
| **理由** | Session 1 中用 Python 腳本批次更新客戶行業分類。但此功能已被 customer-intel skill 的 Nexus 同步流程涵蓋，不需另立 skill。 |
| **評等理由** | 已被 customer-intel 涵蓋 → Weak |

---

## 結論

這批 session 的主要價值在於**驗證既有 skill 的實際效果**，而非發現全新模式：

1. **customer-intel** — 穩德文具印刷的完整流程跑順，確認 WebSearch → 報告 → Nexus 同步的管線可靠
2. **investment-research** — Continuous Mode 每日監控運作正常，portfolio-state.json 的狀態管理設計合理
3. **session-harvest** 本身 — 本次 harvest 也驗證了「不是每次都會有新 skill」是正常的

**建議行動：**
- `nexus-fullstack-feature` 暫不建立，但在 `.learnings/LEARNINGS.md` 記錄此模式，等第二次出現再評估
- 持續觀察 sales-assistant 的開發 session，若全端功能建立模式重複 3 次以上再提取為 skill
