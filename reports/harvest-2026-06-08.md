報告已寫入 `reports/harvest-2026-06-08-sessions.md`。摘要如下。

## 本批 7 場 session 的收割結論

**3 場已被現有 skill 完整覆蓋**：`code-review`（session 2）、`workflow-retro`（session 6）、`taiwan-news-classifier`（session 7）。`TWstock-invest` 四場貢獻兩個正交新訊號，`mops-dbs` 一場為弱訊號。

### Skill 候選（依強度排序）

| 強度 | 名稱 | 核心 | 跨幾場 |
|---|---|---|---|
| **Strong** | `post-backfill-indicator-recompute` | 補完 XBRL 後**只重算 valid<6 的公司**、滿格沿用、產生 funnel 一致新快照、regressed 強制 False | 1（但有完整 docstring 的可複用腳本）|
| **Moderate** | `all-market-universe-empty-diagnose` | 切 `universe=all` 後前端全空、清快取無效的逐段除錯（快照是否含該 universe／cache key 是否帶 universe 維度）| 2（session 3+4，最重的除錯主題）|
| **Weak** | `mops-cluster-schedule-status` | 唯讀回報各 DB 最近/下一期 update 時間 | 1（建議併入既有 audit skill，不獨立）|

### 兩個判斷重點
- **候選 1 與既有 skill 正交**：`stock-fundamentals-grading` 管「怎麼算指標」、`stock-data-gap-diagnose` 管「單股資料不足根因」，而本候選管「批次補資料後如何只重算受影響子集」——那三條決策規則（滿格沿用／缺格重算／regressed=False）值得固化，建議獨立成案。
- **候選 2 與 `stock-data-gap-diagnose` 同類不同軸**：前者是「單股」、後者是「整個 universe 維度切換後全空」。我傾向 Moderate，次選是把這條 universe checklist 增補進既有 skill，避免疊床架屋。

`mops-cluster-master-alignment-audit`（上一批的 Strong 候選）我確認已建立並出現在可用 skill 清單中，所以本批 session 5 的弱訊號就掛在它底下，不另立。

需要我把候選 1 直接用 `skill-creator` 落成 SKILL.md 嗎？
