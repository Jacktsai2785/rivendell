# Session Harvest Report — 2026-05-11

**分析週期**：2026-05-07 ～ 2026-05-11  
**Session 數**：8 | **訊息總數**：~389 | **使用費用**：$0.00  
**分析範圍**：`-home-jacktsai-taiwan-company`（5 sessions）、`-home-jacktsai`（3 sessions）

---

## Session 摘要

| # | 專案 | 訊息 | 主要活動 | 關鍵工具 |
|---|------|------|----------|----------|
| 1 | taiwan-company | 5 | 五百戶科技投資備忘錄（PE memo） | WebFetch, WebSearch |
| 2 | taiwan-company | 20 | 環穎永續發展科技 PE 初步盡調 | WebSearch(11), WebFetch(7) |
| 3 | taiwan-company | 30 | 光泰寰宇 PE 盡調 + tw-company-lookup skill | WebSearch, WebFetch, Bash, Skill(1) |
| 4 | taiwan-company | 9 | 中鼎工程 PE 盡調 | WebSearch(5), WebFetch(2) |
| 5 | taiwan-company | 15 | 嘉盈鋰源 PE 盡調（新創鋰電池公司） | WebSearch(8), WebFetch(5) |
| 6 | rivendell | 241 | mops_rev 全棧管理後台從 SPEC.md Phase 0→5 | Bash(110), Write(81), TodoWrite(10) |
| 7 | rivendell | 11 | 查詢 mops_rev 服務位置與匯出檔案路徑 | Bash(4) |
| 8 | rivendell | 58 | 前後端 Port 衝突排查、修正 Vite API 設定 | Bash(45), Edit(3), Agent(1) |

---

## 技術觀察

### Sessions 1–5：台灣公司 PE 盡調（重複模式）

5 個 session 全部遵循相同工作流：
1. 接收公司基本資料（公司名、統編）
2. WebSearch 搜尋公開資訊（MOPS、官網、新聞）
3. WebFetch 抓取 MOPS 公司基本資料頁
4. 整合資訊撰寫結構化投資備忘錄

Session 3 額外使用了 `tw-company-lookup` skill 補充登記資料。

**現有涵蓋**：`tw-company-pe-memo`（已存在，但為 stub，尚未填入實作細節）

### Session 6：MOPS Rev 全棧管理後台（241 訊息）

最重要的 session。核心任務：讀取 `SPEC.md`，依序實作 Phase 0–5，建立完整 React + Vite 前端搭配 Python 後端的管理後台。

- **創建檔案**：`App.tsx`、`CategoryAdminPage.tsx`、`.env`、`.env.example`、`DEPLOYMENT.md`
- **工具分布**：Bash(110) 為主，Write(81) 建立大量新檔案，TodoWrite(10) 追蹤 phase 進度
- **核心模式**：讀 SPEC.md → TodoWrite 標記 phase → Bash 建環境 → Write 建前後端檔案 → 逐 phase 驗證

### Sessions 7–8：多服務 Port 衝突排查

- Session 7：用 `ps aux`, `lsof -i`, `ls` 查 mops_rev 進程與匯出路徑（純診斷）
- Session 8：`taiwan_company` 前端與 `web_mops_rev` Port 衝突 → 調整 `web_mops_rev` Port → 修正 `frontend/src/api/client.ts` 中的 `baseURL` → 確認 Vite HMR 自動重連

---

## Skill 候選

### [Moderate] `phased-spec-builder` — 從 SPEC.md 分階段建立全棧 Web 應用

| 項目 | 內容 |
|------|------|
| **目的** | 讀取含分階段定義的 SPEC.md，用 TodoWrite 追蹤進度，依序實作 Phase 0→N，建立 React+Vite 前端 + Python/Node 後端，最終輸出 DEPLOYMENT.md |
| **觸發時機** | 「請從 SPEC.md 開始實作」、「Phase 0 到 Phase N」、「按照 spec 建 web app」 |
| **類別** | `workflow/` |
| **資料來源** | Session 6（241 訊息，最大 session）|
| **理由** | Session 6 展示了一個非常清晰的分階段建構流程：TodoWrite 作為 phase 追蹤器、每個 phase 結束用 Bash 驗證、.env/.env.example 搭配 DEPLOYMENT.md 作為交付物。這個模式在任何「有 SPEC + phases」的全棧建構場景均可複用，與 `executing-plans`（執行已有計畫）不同，此 skill 強調「邊讀 SPEC 邊建構」的遞增式實作。 |
| **不重疊** | `mops-rev-scraper`（資料爬取）、`taiwan-financial-category-display`（顯示邏輯）；rivendell skills 目錄中無 `executing-plans` |

---

### [Moderate] `dev-port-conflict-fix` — 本地多服務 Port 衝突診斷與修正

| 項目 | 內容 |
|------|------|
| **目的** | 當多個本地 web 服務 Port 衝突時，用系統指令定位衝突根因，調整其中一個服務的 Port，並自動更新前端 API client 的 `baseURL` 設定 |
| **觸發時機** | 「前端停了」、「port already in use」、「服務衝突」、「前端打不到後端」 |
| **類別** | `meta/` |
| **資料來源** | Session 7（診斷）+ Session 8（修正）|
| **理由** | 兩個 session 共同呈現一個完整的除錯流程：`ps aux / lsof -i` 定位服務 → 識別衝突 Port → 修改衝突方設定（Vite config 或後端 PORT env）→ 修正前端 `api/client.ts` 的 `baseURL` → 確認 Vite HMR 重連。步驟固定、常見，值得封裝。Vite HMR 重連的確認步驟尤其是常被遺漏的細節。 |
| **不重疊** | rivendell skills 目錄中無 `env-doctor`；即使存在於其他位置，env-doctor 聚焦環境變數，此 skill 聚焦 Port 衝突與 API client 設定修正 |

---

### [Weak] 補全 `tw-company-pe-memo`（現有 Skill 強化需求，非新 skill）

| 項目 | 內容 |
|------|------|
| **現況** | Skill 已存在，但 SKILL.md 內容為空殼（`## TODO`）|
| **建議** | 以 Sessions 1–5 的實際工作流補全：搜尋順序（MOPS 登記 → 官網 → 新聞媒體）、備忘錄 sections（業務概況、財務概況、市場定位、投資風險、初步評估）、常見坑（公司未上市時無 MOPS 財報頁、統編搜尋結果為空時改用公司名稱搜尋）|
| **理由** | 本週被呼叫 5 次，是使用頻率最高的工作流，但仍是空殼。不是新 skill 候選，而是現有 skill 的補強需求。 |

---

## 建議優先順序

1. **補全 `tw-company-pe-memo`**：5 次高頻使用，只需把 sessions 1–5 的流程寫入 SKILL.md，立即可提升品質
2. **建立 `phased-spec-builder`**：Session 6 的 241 訊息展示清晰可複用的建構模式，適合任何有 phases 的全棧專案
3. **建立 `dev-port-conflict-fix`**：小而精確，Vite HMR 重連的細節特別值得記錄，避免重蹈覆轍

---

*Generated by session-harvest skill — 2026-05-11*
