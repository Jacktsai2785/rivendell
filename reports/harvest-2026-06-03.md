報告寫入 `reports/harvest-2026-06-03-batch2.md`。

**核心發現**：30 個 session 全部集中在台灣 PE 盡職調查領域。7 個 silent-fail（msgs=1, tools=0），21 個跑完完整 DD，但只有 1 個顯式 invoke `tw-company-pe-memo` skill。共宅一生重複 6 次（與 2026-06-01 的「玩艸植造 ×6、律果科技 ×6」同樣態）。

**Skill 建議**：

| 強度 | 名稱 | 目的 |
|---|---|---|
| Weak | `pe-memo-toolsearch-warm-cache` | 22 次 ToolSearch 卻只 invoke 1 次 Skill，schema cache 未被利用 |
| Weak | `pe-memo-websearch-query-tuner` | WebSearch:WebFetch ~2:1，query 精準度可優化 |
| Skip | （其他） | 此領域已被 18+ 個現有 skill 覆蓋 |

**反向 takeaway**：PE DD 領域過度 skill 化，新增只會增加 routing 混亂。真正的瓶頸是 silent-fail 比例（本批 23%、昨日 33%），更適合用 `investigate` 追根因而非再加 skill。
